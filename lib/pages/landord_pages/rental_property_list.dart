import 'package:flutter/material.dart';

class RentalPropertyList extends StatelessWidget {
  const RentalPropertyList({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}